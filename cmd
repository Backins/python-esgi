install flask-sqlalchemy
install mysqlclient
install evenlet
install flask-socketio

python version 3.4
mysqlclient version 1.3.6

configure database with docker address

https://www.scivision.co/python-windows-visual-c++-14-required/

utilisation de scrapy qui sert a crawler un site a partir d'une url

startproject lenomdetonprojet
// creer un dossier avec le nom de ton projet

cd lenomduprojet

settings.py = param du bot
        item pipeline - le code passe dans le pipeline
        sert a faire un traitement de l'item retourner par le spider
/spiders = logique du code - recherche page et cie ..


lancer le spider

scrapy crawl lenomdefinidanslescript -o nomdufichieroutput.json

route process method = post

installer anaconda pour pouvoir installer le package scrapy qui est une librairie pour crawler un site


puis install conda-forge pour les packages scrapy
conda config --add channels conda-forge
conda install -c conda-forge scrapy

action jquery crawler -
    quand il est soumis envoi du message au websocket soumet moi ça.
    envoi le message a flask et ne soumet pas le formulaire


verification si deux processus sont lancé en meme temps ou non

faire les verifications pour voir si l'url est correct en js

scrapper.py est le script qui crawl et on l'importe

github/scrapinggithub

parse de l'url rentrer grace a urllib pour avoir une url propre

lancer avec conda
